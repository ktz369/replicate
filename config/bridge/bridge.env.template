# bridge.env — template konfigurasi pintu Telegram (beye-bridge.py)
#
# Salin ke /opt/data/cutover-bridge/bridge.env pada mesin live, lalu isi
# nilai aslinya. JANGAN commit nilai asli ke repo ini.
#
# Sumber struktur: /opt/data/cutover-bridge/bridge.env (live).
# CATATAN: file live juga memuat API key provider (MOONSHOT_API_KEY,
# DEEPSEEK_API_KEY, GOOGLE_API_KEY, CURSOR_API_KEY) — itu kredensial dan
# sengaja TIDAK diikutkan di template ini; isi manual per mesin.

TELEGRAM_BOT_TOKEN=<TELEGRAM_BOT_TOKEN>
TELEGRAM_HOME_CHANNEL=<TELEGRAM_HOME_CHANNEL>
BRIDGE_OPERATOR_UID=<BRIDGE_OPERATOR_UID>
PI_AGENT_DIR=<PI_AGENT_DIR>
PI_TIMEOUT=<PI_TIMEOUT>
PI_CWD=<PI_CWD>
